<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Read User</title>
  <!-- Include Tailwind CSS -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100 h-screen p-4">

  <div class="bg-white shadow-md rounded-lg p-6 w-full ">
    <a href="/" class="text-blue-500">Back to home</a>
    <h2 class="text-2xl mt-10 font-bold mb-4 text-center">All Users</h2>
    <div class="users flex flex-wrap flex-align-start">

      <%if(users.length > 0) { %>
      <% users.forEach(function(user) { %>
        <div class="user max-w-72 p-10 m-0.5 bg-gray-200 rounded-lg">
          <div class="w-full h-52 rounded-lg overflow-hidden">
            <image class="w-full h-full object-cover " src="<%= user.image%>">
          </div>
          <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
          <h5 class="text-gray-500"><%= user.email%></h5>
          <div class="flex gap-3 mt-10">
            <a href="/edit/<%= user._id%>" class="text-gray-600">Edit User</a>
            <a href="/delete/<%= user._id %>" class="text-red-500">Delete User</a>
          </div>
        </div>
      <% })%>
      <% } else {%>
        <h3 class="text-zinc-500">No user yet.</h3>
      <% } %>

    </div>
  </div>

</body>

</html>